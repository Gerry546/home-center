header:
  version: 13
  includes:
  - kas/common-kas.yml
# The machine as it is written into the `local.conf` of bitbake.
machine: qemu-homely-arm64

target: 
  - home-center-image 
  # - home-center-bundle-qemu